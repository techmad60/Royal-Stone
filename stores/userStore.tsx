// import create from 'zustand';

// const useUserStore = create((set) => ({
//   userDetails: null,
//   setUserDetails: (details) => set({ userDetails: details }),
// }));

// // In your signup/login flow:
// useUserStore.setState({ userDetails: account });
